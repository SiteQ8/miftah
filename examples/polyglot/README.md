# Polyglot fixture

A deliberately bad estate written in the languages the scanner has to read. This
is a fixture, never a template. Every file here is wrong on purpose, and the
tests assert that each one is caught in its own idiom rather than in the
JavaScript spelling of the same algorithm.
